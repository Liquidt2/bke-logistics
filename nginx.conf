server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # Gzip compression
    gzip on;
    gzip_vary on;
    gzip_min_length 1000;
    gzip_comp_level 6;
    gzip_types
        text/plain
        text/css
        text/xml
        text/javascript
        application/javascript
        application/xml
        application/json
        image/svg+xml;

    # Cache static assets
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
        access_log off;
    }

    # Security headers
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;

    # OpenClaw webhook proxy — avoids CORS, keeps token server-side
    location /api/webhook {
        proxy_pass https://klaus.bkelogistics.com/hooks/agent;
        proxy_ssl_server_name on;
        proxy_set_header Host klaus.bkelogistics.com;
        proxy_set_header Authorization "Bearer b31f59bd31db58608bdc6c8418b94edca02eb20af25c526ed4b1a8935f5d9c5f";
        proxy_set_header Content-Type "application/json";
        proxy_set_header X-Real-IP $remote_addr;
        proxy_pass_request_headers off;
    }

    # Handle Astro static routes
    location / {
        try_files $uri $uri/ $uri.html /index.html;
    }

    # Custom 404
    error_page 404 /404.html;
    location = /404.html {
        internal;
    }
}
