---
interface Props {
  icon?: string;
  title: string;
  description: string;
  href: string;
  featured?: boolean;
}

const { icon, title, description, href, featured = false } = Astro.props;
---

<a
  href={href}
  class={`group card-service flex flex-col gap-5 ${featured ? 'border-blue-brand/30' : ''}`}
>
  {icon && (
    <div class="w-12 h-12 rounded-sm bg-navy/50 border border-navy-light/20 flex items-center justify-center text-2xl group-hover:bg-blue-brand/10 group-hover:border-blue-brand/30 transition-all duration-300">
      {icon}
    </div>
  )}

  <div class="flex-1">
    <h3 class="font-heading font-bold text-2xl text-text-primary mb-2 group-hover:text-blue-brand transition-colors">
      {title}
    </h3>
    <p class="text-text-muted text-sm leading-relaxed">
      {description}
    </p>
  </div>

  <div class="flex items-center gap-2 text-blue-brand text-sm font-semibold mt-auto pt-2 border-t border-white/5">
    <span>Learn More</span>
    <svg
      class="w-4 h-4 transition-transform duration-200 group-hover:translate-x-1"
      fill="none" stroke="currentColor" viewBox="0 0 24 24"
    >
      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2.5" d="M17 8l4 4m0 0l-4 4m4-4H3"/>
    </svg>
  </div>
</a>
